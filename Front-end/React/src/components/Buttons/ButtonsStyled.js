import styled from 'styled-components';

export default styled.div`
  .Button
  text-align: left;
    margin-top: 1em;
    display: flex;
    flex-wrap: wrap;
    justify-content: space-around;
  }

  

`;
